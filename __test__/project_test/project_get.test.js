describe("GET /project/:id tests", () => {
    test.todo("handles :id that does not exist")
    test.todo("admin has acces even if it's not assigned")
    test.todo("handles user not assigned to proj")
    test.todo("returns project info with :id")
})